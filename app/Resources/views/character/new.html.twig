{% extends 'base.html.twig' %}

{% block body %}
    <a href="{{ path('project_show', {'id': projectId }) }}" class="btn btn-primary" ><i class="fa fa-arrow-left"></i> Back to the project</a>

    <h1>Character creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="hidden" name="projectId" value="{{projectId}}" />
        <input type="submit" value="Create" />
    {{ form_end(form) }}

{% endblock %}
